node_modules/
dist/
*.local
.env
.env.*
!.env.example

# Runtime caches produced by the server
.cache/
art-cache/

# Generated from vendor/opensource at build time by desktop/scripts/make-icon.mjs
desktop/build/

# OS noise
Thumbs.db
.DS_Store

# Tooling scratch. The Impeccable config and critiques are tracked; this is a
# per-machine record of which files the detector hook has already seen.
.impeccable/hook.cache.json

# design-sync: staged converter scripts, generated bundle, machine-local state.
# Everything else under .design-sync/ is a committed sync input.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
client/.ds-lib/
client/ds-types/
client/index.d.ts
