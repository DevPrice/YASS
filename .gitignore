node_modules/
dist/
*.local
.env
.env.*
!.env.example

# Runtime caches produced by the server
.cache/
art-cache/

# OS noise
Thumbs.db
.DS_Store

# Tooling scratch. The Impeccable config and critiques are tracked; this is a
# per-machine record of which files the detector hook has already seen.
.impeccable/hook.cache.json
